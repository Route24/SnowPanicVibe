using UnityEngine;

/// <summary>
/// Safe 基準の識別子を静かに保持するだけ。判定・復元・ログなし。
/// </summary>
public sealed class SafeStateSnapshot : MonoBehaviour
{
    internal const string SafeStateVersion = "TASK13_SAFE";
}
